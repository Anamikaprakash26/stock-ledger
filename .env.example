# Local dev uses SQLite — zero external setup.
# For a production deploy, switch the datasource provider in prisma/schema.prisma
# to "postgresql" and point this at a Postgres/Neon connection string.
DATABASE_URL="file:./dev.db"
