# Postgres connection string. Use a free database from https://neon.tech
# (use the Direct/unpooled string so migrations run cleanly).
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"
